'use strict'

// This time we have to specify the filename without the ".js" extension. Since the function we want doesn't reside
// in the default index.js
const utils = require('../utils/demo2a')

utils.print("hello demo2")